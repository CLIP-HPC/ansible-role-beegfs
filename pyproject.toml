[project]
name = "ansible-role-beegfs"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "ansible>=12.3.0",
    "ansible-lint>=26.3.0",
    "molecule-plugins[vagrant]>=25.8.12",
    "python-vagrant>=1.0.0",
]
